name: vinyl_sound_oficial
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.4.1 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.2
  just_audio: ^0.9.39
  just_audio_windows: ^0.2.1
  provider: ^6.1.2
  shared_preferences: ^2.3.2
  youtube_explode_dart: ^2.2.1

dev_dependencies:
  flutter_lints: ^3.0.0
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
